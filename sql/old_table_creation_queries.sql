
CREATE TABLE Paths {
    -- path_id VARCHAR(255) PRIMARY KEY,
    -- trip_id VARCHAR(255),
    -- departure_stop INT,
    -- arrival_stop INT,
    -- departure_time TIME,
    -- arrival_time TIME,
    -- FOREIGN KEY(trip_id) REFERENCES Trips(trip_id),
    -- FOREIGN KEY(trip_id) REFERENCES StopTimes(trip_id),
    -- FOREIGN KEY(departure_stop) REFERENCES Stops(stop_id),
    -- FOREIGN KEY(arrival_stop) REFERENCES Stops(stop_id)
}